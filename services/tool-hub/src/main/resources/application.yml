server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bizflow
    username: bizflow
    password: bizflow
  jpa:
    hibernate:
      ddl-auto: none

management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics"
